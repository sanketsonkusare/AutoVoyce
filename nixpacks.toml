# Nixpacks configuration for Railway
# This installs ffmpeg which is required by yt-dlp for audio extraction

[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
